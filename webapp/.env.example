# URL de l'API backend (serveur Express).
# En développement local : laisser la valeur ci-dessous.
# En production : remplacer par l'IP/domaine du serveur.
VITE_API_URL=http://localhost:5000/api

# Gateway LLM interne (optionnel, configurée côté serveur).
# Aucune clé LLM n'est exposée au frontend.
