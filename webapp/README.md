# IAM — Portail Logistique & Gestion des Chauffeurs

Interface web (Vite + React + TypeScript) du portail IAM.

## Démarrage

```bash
npm install
npm run dev
```

Le serveur de développement démarre sur le port 5173 et proxifie `/api` vers le backend Express (port 5000).
